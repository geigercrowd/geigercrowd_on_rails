= form_for(@user) do |f|
  %fieldset
    .label= f.label :email, t('.email')
    = f.text_field :email
  %fieldset
    .label= f.label :password, t('.password')
    .float_left= f.password_field :password
    .gray= t('optional')
    .clear
    .label= f.label :password_confirmation, t('.password_confirmation')
    = f.password_field :password_confirmation

    .actions
      = f.submit
